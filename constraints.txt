# 依赖约束文件（与 pyproject.toml 范围一致的已解析版本）
# 生成方式: python3.12 -m venv .venv-lock && pip install -e '.[dev]' && pip freeze
# 生成时间: 2026年 09月 10日 CST
# 用法: pip install -e '.[dev]' -c constraints.txt

# FastAPI 组件（锁定已验证的 0.115.x 单版本线，见 pyproject.toml 说明）
fastapi==0.115.14
starlette==0.46.2
uvicorn==0.52.4
pydantic==2.13.5

# SQLAlchemy 组件
sqlalchemy==2.0.52
alembic==1.19.2
psycopg2-binary==2.9.13

# 安全/认证组件
bcrypt==4.0.1
passlib==1.7.4
python-jose==3.5.0

# 其他依赖组件
ldap3==2.9.1
pandas==2.3.3
openpyxl==3.1.5
python-multipart==0.0.32
olefile==0.47

# 开发工具
pytest==8.4.2
httpx==0.28.1
ruff==0.16.7
